import {
  PCMAge,
  PCMArticle,
  PCMArticleType,
  PCMPeriod,
  PCMPeriodType,
  PCMPublish,
} from "../models/pcm"; // Import everything from the barrel file

export const pcmEntities = [
  PCMAge,
  PCMArticle,
  PCMArticleType,
  PCMPeriod,
  PCMPeriodType,
  PCMPublish,
];
